# Public API origin embedded in the app bundle. Production builds require HTTPS.
EXPO_PUBLIC_API_URL=https://api.example.com

# When true (or when /founder/pulse is missing), CEO KPIs load from the local fixture.
EXPO_PUBLIC_FOUNDER_USE_FIXTURE=true
