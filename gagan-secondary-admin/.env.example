# Preview-safe defaults. Do not put secrets in this file.
VITE_DATA_SOURCE=demo
VITE_DEMO_MODE=true

# Only set this to an approved Gagan API origin for a separately provisioned
# environment. Never point it at another company's API.
VITE_API_URL=http://localhost:4000
